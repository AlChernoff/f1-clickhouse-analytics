[project]
name = "f1-loader"
version = "0.1.0"
description = "F1 data replay loader for ClickHouse"
requires-python = ">=3.11"
dependencies = [
    "clickhouse-connect==0.8.7",
    "pandas==2.2.3",
    "python-dotenv==1.0.1",
    "pyyaml==6.0.2",
]
