name: f1_clickhouse_analytics
version: "1.0.0"
config-version: 2

profile: f1_clickhouse_analytics

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  f1_clickhouse_analytics:
    staging:
      +materialized: view
      +schema: dwh
    dwh:
      +materialized: view
      +schema: dwh
    marts:
      +materialized: view
      +schema: marts
